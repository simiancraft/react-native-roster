[install]
linker = "hoisted"

[test]
coverageThreshold = 1.0
coverageSkipTestFiles = true
coveragePathIgnorePatterns = ["**/dist/**", "**/demo/**", "scripts/set-version.ts"]
# The CLI shim is exercised in a subprocess; its imported version writer is coverage-gated.
