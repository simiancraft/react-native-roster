# Issue 217 command receipt

Capture commit: `5ad26caf605f702536da4793cabcc9dd8e18f9fd`

Remote verification:

```text
$ git fetch origin fix/actionable-day-headers-217
$ git merge-base --is-ancestor HEAD origin/fix/actionable-day-headers-217
exit 0
```

Focused native interaction coverage, captured after the final push:

```text
$ bun test test/components/schedule/render.test.ts
19 pass
0 fail
135 expect() calls
Ran 19 tests across 1 file.
```

CI-equivalent repository gate, run at the capture commit after merging current `origin/main`:

```text
$ bun install --frozen-lockfile
Checked 1147 installs across 1264 packages (no changes)

$ bun run check
All files: 100.00% functions, 100.00% lines
2776 pass
0 fail
32578 expect() calls

react-native-roster: 54.26 kB of 54.26 kB
react-native-roster/nativewind: 50.28 kB of 50.28 kB

Day header: pointer, Enter, and Space each activate the actual day exactly once.
LaneRow profiler: 14 continuously mounted lanes, zero updates on the second scroll pass; mount and update controls pass.
Selection profiler: zero mounted-row updates when opening details or dismissing on a cell.
exit 0
```

The repository CI workflow runs the same frozen install, lint, three typechecks, React Compiler
check, build, web export and browser check, size check, strict package check, dead-code check, and
coverage test commands that `bun run check` exercised.
